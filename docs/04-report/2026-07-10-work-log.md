---
tags: [report, worklog, round/theme-ui, ops/timeline, status/done]
---

# BarroAiTrade 작업 기록 — 2026-07 (테마 데이터·티마 UI 라운드 + 운영 활동 로그)

> **Summary**: 이번 개발 라운드(테마 데이터 3계층 아키텍처 정합 + 티마 대시보드 종목상세/호가창 UI 정합, PR [#211])의 커밋 7건과, 이를 둘러싼 2026-05~07 운영 활동 타임라인·주요 인시던트·현재 운영 상태를 한 문서로 정리한 작업 기록. **전 구간 키움 mockapi 위 mock-live(실금 0)** — 본 라운드는 데이터/표시·UI 계층 변경으로 주문·매매·계좌 경로는 무변경(관찰).
>
> **Date**: 2026-07-10 · **Status**: Done(기록). 성과 수치는 % / 승률 / PF 등 비율 지표로만 기재하며, 원화 손익 금액은 신뢰불가로 판단해 의도적으로 생략함.

---

## 1. 이번 개발 라운드 — 테마 데이터·티마 UI 정합 (PR [#211])

- **브랜치**: `worktree-sequential-snacking-harbor` · **분기점**: main `b6672a1`
- **PR**: [#211](https://github.com/82beye/BarroAiTrade/pull/211) `fix(themes/stocks): 테마 데이터 아키텍처 정합 + 종목상세/호가창 UI 정합` (OPEN)
- **출처 분석 문서**: [[2026-07-08-theme-implementation-issues-and-fix-design]] · `03-analysis/2026-07-08-theme-data-implementation-analysis.md`(미커밋 로컬 문서)

### 1.1 커밋 (b6672a1..HEAD, 오래된 순)

| # | SHA | 요약 |
|---|-----|------|
| 1 | `edb1b1a` | 분석문서 발견 4건 구현 — **(P0)** `theme_board_cache` enrich 기본값 0→1로 라이브 시세 표시 회귀 복원, **(P0)** `finup_importer` wipe를 `__source:finup__` 마커로 자기소유 테마만 삭제하도록 한정 + upsert-by-name 통일, **(P1)** `market_hours.is_open_rush()` 신규로 개장 직후(09:00~09:05) 배경잡 유예, **(P2)** 뉴스발굴이 `market_row_store` 재사용해 후보 유니버스 중복 제거. 테스트 12건 추가 |
| 2 | `362d336` | enrich 복원 후 실측 재조정 — 429 누적으로 1사이클 4.5분 정체 → `_THEME_QUOTE_CAP` 20→8, 캐시 주기 20s→120s |
| 3 | `125faee` | 테마보드 카드 정렬을 `change_pct` 평균 → `market-aggregates` `rank_by_value`(오늘 거래대금 랭킹)로 교체. 집계 실패 시 기존 정렬로 조용히 강등, 스냅숏(과거 동결)은 미적용 |
| 4 | `534243b` | `GET /api/market/ticker/{symbol}` 시세 전체 실패 시 이름까지 버리고 404 → 로컬 이름 마스터로 `source:"name_only"`(price=null) 강등, 진짜 미상만 404. 테스트 2건 |
| 5 | `8f7b0bc` | 호가창 참조가 패널 6행(기준가/시가/고가/저가/상한가/하한가)을 '라벨→값' → 레퍼런스의 '값→라벨' 순으로 정렬. 상단 거래대금은 백엔드 응답에 필드 부재로 미구현 명시 |
| 6 | `a8d575d` | 종목상세 헤더 압축 — 2행 카드를 좌우 한 줄(`justify-between`)로, 폰트/패딩 축소 → 호가창이 스크롤 없이 한 화면에 표시 |
| 7 | `61753ca` | `tima-shell`에서 `showSearch = pathname === '/themes'` 게이팅 — 메인 외 서브 페이지 상단 종목 검색 row 숨김(알림·햄버거 버튼은 전 페이지 유지) |

### 1.2 변경 범위

- **백엔드(데이터/표시 계층)**: 테마 파이프라인 스케줄러 잡(`theme_board_cache`/`discovery`/`market_row`)·`market_hours`, `themes`의 `finup_importer`·`news_theme_discovery`, `api/routes`의 `themes_calendar_news`·`market`, 관련 테스트(`test_finup_importer`·`test_market_hours`·`test_market_ticker_fallback`).
- **프론트(티마 대시보드 UI)**: `themes`·`stocks/[symbol]` 페이지, `tima-shell`, `lib/api.ts`의 정렬·호가창·헤더·검색 row.
- **주문·매매·계좌 경로**: 무변경 — 안전 경계(§2) 준수, 커밋 전 `git status` S5 점검 클린.

### 1.3 검증 결과 (실측)

- **타깃 테스트 슬라이스** (`-k "finup_importer or market_hours or theme_market_row_store or market_ticker_fallback"`): **23 passed**.
- **전체 회귀** (`pytest backend/tests -q`): **1884 passed / 3 failed / 10 skipped**. ⚠️ 실패 3건은 이 브랜치 커밋(`b6672a1..HEAD`)이 건드리지 않은 파일(`test_force_close_exempt.py`, `test_ohlcv_exchange.py`)의 **기존·환경종속 실패** — 후자 2건은 이 머신의 홈 경로(`/Users/beye82/...`)를 하드코딩 가정과 비교하는 테스트라 무관함을 `git diff --name-only`로 확인. **regression 아님**(별도 이슈화 필요).
- **프론트 타입체크** (`tsc --noEmit -p frontend`): 0 errors.
- **라이브 curl**: `/themes` 검색 row 노출·`/account`·`/calendar` 미노출; `/api/market/ticker` name-only 강등 동작 확인.
- **(미검증·대기)** 사용자 온디바이스 시각 검증: 호가창 값/라벨 순서, 헤더 압축 후 한 화면 표시 여부.

### 1.4 관련 PR

- [#210] `feat(dashboard): 티마 벤치마크 P1+P2` — 알림센터·타임라인·티커·종목상세·캘린더·호가·검색·NXT (OPEN)
- [#209] `feat(dashboard): 티마 벤치마크 P0` — 전략 스크리너·차트 기준선·테마 보드·면책 (BAR-62b) (MERGED)

---

## 2. 문서화된 발견 · 최근 리포트

> 아래는 최근 산출된 분석/리뷰 문서와 그 핵심 발견. 실거래 반영이 필요한 항목은 대부분 HITL(사람 승인) 게이트로 남아 있음.

| 문서 | 요지 |
|------|------|
| [[2026-07-08-theme-implementation-issues-and-fix-design]] | 미커밋 테마 병행작업 실측 검증 — 라이브 시세 P0 회귀·Finup wipe-and-replace·배경잡 429 경합 우선순위 재정리 |
| `03-analysis/2026-07-08-theme-data-implementation-analysis.md`(미커밋 로컬) | 테마 데이터 3계층(DB 마스터·표시용 캐시·키움 랭킹 row CSV 집계) 구조와 한계 5종 자가진단 |
| [[2026-07-02-trade-log-analysis-tuning]] | `fill_audit` 487 라운드트립 실측 — supertrend PF 0.95(활성 최선)·limit_up_chase PF 0.01 LIVE 중단·closing_bet 자동매도 부재 |
| [[2026-07-01-strategy-review]] | 멀티에이전트 전략 리뷰 58건(CONFIRMED 39) — 종베 데몬 크래시버그·리스크게이트 분절·swing_38 add_on 미구현·GBM 합성 백테스트 한계 |
| [[2026-07-01-security-review]] | 보안 리뷰 37건(CONFIRMED 35) — 백엔드 API 무인증이 ngrok 공개터널로 노출돼 인증 없이 계좌·포지션 조회 가능 |

**핵심 발견 요약**

- **[테마]** 표시용 캐시(항상 enrich)와 온디맨드 상세(`?enrich=true`)의 책임 분리가 근본책. `finup_importer`의 무조건 wipe가 큐레이션 21종을 Finup 스냅숏 30종으로 치환했던 이력(현재 스케줄러 미등록이라 반복 소실은 없음).
- **[테마]** 대시보드 배경잡이 실거래 데몬과 동일 키움 레이트리밋 예산을 공유 → 개장 직후 429 연쇄. OPEN-RUSH-YIELD 유예로 완화.
- **[전략]** 활성 전략 중 supertrend가 실측 PF 0.95로 최선. `limit_up_chase`는 PF 0.01로 LIVE 중단. `closing_bet`은 평균손실 큰데 자동매도 미구현(알림전용).
- **[보안]** mock이라 금전손실은 없으나 `KIWOOM_BASE_URL` 실서버 전환 시 무단 주문 가능 구조 → JWT 배선·라우터 인증·터널 차단이 실금 전환 전 선행 필수(HITL).
- **[일일리포트]** `04-report/daily/*.md`는 매일 07:00 자동생성 시뮬 누적 history. 최근 누적 승률: `sf_zone` 67.8% · `f_zone` 53.9% · `swing_38` 52.5% · `gold_zone` 47.9%.

---

## 3. 운영 활동 타임라인 (2026-05~07)

> auto-memory 기반 요약. 계좌번호·토큰·키 등 민감정보는 제외, 사건·전략·날짜·결과 수준으로만 기재.

| 날짜 | 구분 | 활동 |
|------|:---:|------|
| 05-31 | ops | launchd 운영 토폴로지 구성(백엔드·봇·대시보드·ngrok, RunAtLoad+KeepAlive) |
| 06-01 | strategy | Supertrend 09:00 개장 예외 진입 통합(수동 적용) |
| 06-01→02 | incident | 일일손익 게이트 KillSwitch 오발동 → balance_history 파일 의존 제거·브로커 평가수익률로 교체(`a9bcdc3`) |
| 06-02 | incident | 동전주 수량 폭주 → `min_price` 1000원 필터(`c5991b2`) |
| 06-03→04 | incident | 키움 모의 App Key/Secret 실효 → 키 재발급으로 회복 |
| 06-05 | strategy | Supertrend+RSI 자동매매 활성화(관찰→mock-live), RSI 확인필터 OFF·lookback 100 |
| 06-05 | incident | 텔레그램 토큰 충돌(409) → 봇 전용 토큰 분리로 해소 |
| 06-05 | ops | 신규 모의계좌 전환·구계좌 phantom net-0 리셋 |
| 06-08 | strategy | 5전략(supertrend+f/sf/gold_zone+swing_38) 동시 LIVE(mock) |
| 06-08 | incident | 모의 주문 접수정체(미체결) → `ka10075`/SYNC 보존 로직으로 수정 |
| 06-10 | ops | 매수경로 3개 정리·ETF 전면 차단(EXCLUDE_ETF) |
| 06-11 | strategy | 상따(`limit_up_chase`) 라이브(mock) 전환 |
| 06-11→14 | ops | EOD 자동화 크론(OHLCV 갱신→아카이브→이메일), 첨부 차단 → 분할+base64 발송기 교체 |
| 06-12 | strategy | 라운드피겨 손절 보정(`round_figure`) 다수 전략 통합, default-OFF 관찰 |
| 06-15 | incident | 개장러시 429 포화·주문 다발실패 → 상따 OFF·데몬 중지(교훈: 한도↓ 아닌 매수주체 정지가 정답) |
| 06-16 | agent | 손실감시 에이전트 구축·launchd 영속화(90s 감시·픽스 초안은 HITL) |
| 06-16 | incident | 6/15 EOD 데이터 누락 → `eod_recover`로 fill_audit 백필 |
| 06-18 | agent | 종가베팅 알림 데몬 구축(주문 없음·텔레그램 전용) |
| 06-18→22 | incident | 개장 휩쏘 반복손실로 자동매매 중지 → 6/22 잔고 필드 버그(`orderable_cash`, `276dc29`) 픽스와 함께 재개 |
| 06-22 | agent | 단타퀀트 모니터링 에이전트 생성(5렌즈, 장중 자율 감시·HITL) |
| 06-23→24 | agent | 멀티에이전트 협업방 매매복기+수정 6종(default-OFF), advisory-writer 가동, barrotrade 에이전트 11종 통합·협업방 7역할 |
| 06-26 | ops | 협업방·시장국면조사 중지(토큰절감) — 매매데몬·봇·loss-watch는 유지 |
| 07-01 | security | 보안 심층검토 37건 — API 무인증+ngrok 노출 확인, 로그 권한 600 적용 |
| 07-01 | strategy | 매매전략 전반점검 58건 — 종베 데몬 크래시버그 패치, supertrend 라이브 리스크 식별 |
| 07-02 | strategy | 전략 재활성화+튜닝, closing_bet 자동매도 배선(flag OFF시 byte-identical), DCA를 일일손실한도에 편입(`BARRO_DCA_RESPECT_DAILY_LOSS`) |
| 07-03 | strategy | regime 통합 검증·종목당 당일 진입 캡, **단계별 활성화 Phase1** = supertrend 단독→f/sf_zone 추가 |
| 07-06 | feature | 티마 대시보드 21커밋 main 병합, 게이트웨이 버그 3종 수정, 테마캐시 백엔드 배경잡 전환, 뉴스기반 테마발굴(RSS) 가동, /loop 체크인 정규장 한정 |
| 07-08~10 | feature | **본 라운드** — 테마 데이터 아키텍처 정합 + 티마 종목상세/호가창 UI 정합 (PR [#211]) |

---

## 4. 현재 운영 상태 (2026-07 기준)

- **모드**: 키움 `mockapi` 위 **mock-live** — 실금 0. `KIWOOM_BASE_URL` 실서버 전환 및 실키 투입은 사용자 HITL 판단(운영 머신 별도).
- **활성 전략** (7/3 단계별 활성화 정책):
  - `supertrend` (봇, mock-live)
  - `f_zone` (intraday 데몬)
  - `sf_zone` (intraday 데몬 — PF 최약, 관찰 권고)
  - `closing_bet` (알림전용 · 자동실행 OFF)
  - `limit_up_chase`·`gold_zone`·`swing_38` = **OFF**
- **상시 가동(launchd)**: 백엔드(:8000) · 텔레그램봇 · 대시보드(:3000) · loss-watch 감시 에이전트 · 종베 알림 데몬.
- **중지 상태**: agent-room · advisory-writer (6/26부터 토큰절감).
- **/loop 자율 시장 체크인**: 정규장 시간에만 전송(안전 Monitor는 상시).

---

## 5. 주요 인시던트 & 해결

| 날짜 | 인시던트 | 해결 |
|------|----------|------|
| 06-01 | 일일손익 게이트 KillSwitch 오발동(가짜 손실 매수차단) | balance_history 파일 의존 제거·브로커 계좌 평가수익률로 게이트 입력 교체(`a9bcdc3`) |
| 06-03 | 키움 모의 App Key/Secret 실효로 OAuth 실패 | 원인이 계좌 아닌 키 거부임을 규명, 6/4 재발급으로 회복 |
| 06-08 | 모의 주문 접수정체(ORDERED이나 미체결) | mockapi 미체결+잘못된 TR(kt00004) → `ka10075`(oso) 미체결조회+SYNC 보존으로 수정 |
| 06-15 | 개장러시 429 포화·주문 다발실패+한도초과 플러드 | 상따 OFF+데몬 중지로 매수주체 정지(교훈: 한도↓ 아닌 주체 정지) |
| 06-16 | 6/15 EOD 데이터 누락(fill_audit·balance 미기록) | `eod_recover`로 fill_audit 백필(FAIL 2→1). balance_history EOD는 과거잔고 API 부재로 복구 불가 |
| 06-22 | 모의 잔고 필드 버그(매수가능액 0 오판) | 모의 매도대금 예수금 미반영 → `orderable_cash` 우선 사용(`276dc29`) |
| 07-01 | 백엔드 API 전체 무인증+ngrok 공개노출 (CRITICAL) | 로그 권한 600 적용. JWT 배선·라우터 인증·터널 차단은 실금 전환 전 선행 필수 HITL 권고(리포트 main 미머지) |
| 07-02 | DCA 물타기가 일일손실한도 서킷브레이커 우회 | `BARRO_DCA_RESPECT_DAILY_LOSS`로 DCA를 일한도에 편입(default OFF·활성 적용) |

---

## 6. 관련 링크

- [[README|🏠 Knowledge Base Home]] · [[status-dashboard|📊 PDCA Dashboard]] · [[features-index|📇 피처 인덱스]]
- 리포트: [[2026-07-02-trade-log-analysis-tuning]] · [[2026-07-01-strategy-review]] · [[2026-07-01-security-review]]
- 분석: [[2026-07-08-theme-implementation-issues-and-fix-design]] · `03-analysis/2026-07-08-theme-data-implementation-analysis.md`(미커밋 로컬 문서)
- PR: [#211](https://github.com/82beye/BarroAiTrade/pull/211)

---

*작성: 2026-07-10 · 자동 수집(git/PR·auto-memory·docs 리포트) 후 합성. 모든 성과 지표는 비율(%/승률/PF)로만 기재, 원화 손익 금액은 신뢰불가로 생략.*
